import styled from 'styled-components'

export const Header = styled('header')`
  min-height: 90px;
  width: 100%;
  background: #363607;
  max-width: 1200px;
  margin: auto;
  position: fixed;
  left: 50%;
  transform: translateX(-50%);
  top: 40px;
  border-radius: 8px;
`
